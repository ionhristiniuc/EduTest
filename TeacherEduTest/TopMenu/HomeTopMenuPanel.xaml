<UserControl x:Class="TeacherEduTest.TopMenu.HomeTopMenuPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid Margin="5,5,5,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="0" Name="HomeButton" Click="HomeButton_OnClick" Margin="5,0,5,0">Home</Button>
        <Button Grid.Column="1" Name="AddCourseButton" Margin="5,0,5,0">Add Course</Button>
        <Button Grid.Column="2" Name="AboutButton" Margin="5,0,5,0">About</Button>
    </Grid>
</UserControl>
